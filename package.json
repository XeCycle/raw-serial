{
  "name": "raw-serial",
  "version": "0.0.1",
  "description": "Simple wrapper for operating raw serial ports",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "install": "node-gyp rebuild"
  },
  "author": "",
  "license": "ISC",
  "gypfile": true,
  "dependencies": {
    "plexer": "0.0.3"
  }
}
